var frameworks = new Array('Laravel', 'Vue', 'Django')

frameworks.forEach(element => {
    console.log(element)
});

for (let framework of frameworks) {
    console.log(framework)
}

// for (let key in object) {
//     if (object.hasOwnProperty(key)) {
//         const element = object[key];
        
//     }
// }